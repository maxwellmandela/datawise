{
  "wp_users": {
    "columns": [
      "ID (int(11), not nullable, PRI, auto_increment)",
      "user_login (varchar(60), not nullable, , unique)",
      "user_pass (varchar(255), not nullable, )",
      "user_nicename (varchar(50), nullable, )",
      "user_email (varchar(100), not nullable, unique)",
      "user_url (varchar(100), nullable, )",
      "user_registered (datetime, not nullable, default=current_timestamp())",
      "user_status (int(11), not nullable, default=0)",
      "display_name (varchar(250), nullable)"
    ],
    "relationships": []
  },
  "wp_posts": {
    "columns": [
      "ID (int(11), not nullable, PRI, auto_increment)",
      "post_author (int(11), not nullable)",
      "post_date (datetime, not nullable, default=current_timestamp())",
      "post_content (longtext, not nullable)",
      "post_title (text, not nullable)",
      "post_excerpt (text, nullable)",
      "post_status (varchar(20), not nullable, default='publish')",
      "comment_status (varchar(20), not nullable, default='open')",
      "ping_status (varchar(20), not nullable, default='open')",
      "post_name (varchar(200), nullable)",
      "post_modified (datetime, not nullable, default=current_timestamp())",
      "post_type (varchar(20), not nullable, default='post')"
    ],
    "relationships": [
      {
        "column_name": "post_author",
        "referenced_table_name": "wp_users",
        "referenced_column_name": "ID"
      }
    ]
  },
  "wp_comments": {
    "columns": [
      "comment_ID (int(11), not nullable, PRI, auto_increment)",
      "comment_post_ID (int(11), not nullable)",
      "comment_author (varchar(255), not nullable)",
      "comment_author_email (varchar(100), not nullable)",
      "comment_author_url (varchar(200), nullable)",
      "comment_content (text, not nullable)",
      "comment_approved (varchar(20), not nullable, default='1')",
      "comment_date (datetime, not nullable, default=current_timestamp())"
    ],
    "relationships": [
      {
        "column_name": "comment_post_ID",
        "referenced_table_name": "wp_posts",
        "referenced_column_name": "ID"
      }
    ]
  }
}
