{
  "wp_commentmeta": {
    "columns": [
      "meta_id (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "comment_id (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "meta_key (varchar(255), nullable, MUL, , )",
      "meta_value (longtext, nullable, , , )"
    ],
    "relationships": []
  },
  "wp_comments": {
    "columns": [
      "comment_ID (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "comment_post_ID (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "comment_author (tinytext, not nullable, , , )",
      "comment_author_email (varchar(100), not nullable, MUL, , )",
      "comment_author_url (varchar(200), not nullable, , , )",
      "comment_author_IP (varchar(100), not nullable, , , )",
      "comment_date (datetime, not nullable, , default=0000-00-00 00:00:00, )",
      "comment_date_gmt (datetime, not nullable, MUL, default=0000-00-00 00:00:00, )",
      "comment_content (text, not nullable, , , )",
      "comment_karma (int(11), not nullable, , default=0, )",
      "comment_approved (varchar(20), not nullable, MUL, default=1, )",
      "comment_agent (varchar(255), not nullable, , , )",
      "comment_type (varchar(20), not nullable, , default=comment, )",
      "comment_parent (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "user_id (bigint(20) unsigned, not nullable, , default=0, )"
    ],
    "relationships": []
  },
  "wp_links": {
    "columns": [
      "link_id (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "link_url (varchar(255), not nullable, , , )",
      "link_name (varchar(255), not nullable, , , )",
      "link_image (varchar(255), not nullable, , , )",
      "link_target (varchar(25), not nullable, , , )",
      "link_description (varchar(255), not nullable, , , )",
      "link_visible (varchar(20), not nullable, MUL, default=Y, )",
      "link_owner (bigint(20) unsigned, not nullable, , default=1, )",
      "link_rating (int(11), not nullable, , default=0, )",
      "link_updated (datetime, not nullable, , default=0000-00-00 00:00:00, )",
      "link_rel (varchar(255), not nullable, , , )",
      "link_notes (mediumtext, not nullable, , , )",
      "link_rss (varchar(255), not nullable, , , )"
    ],
    "relationships": []
  },
  "wp_options": {
    "columns": [
      "option_id (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "option_name (varchar(191), not nullable, UNI, , )",
      "option_value (longtext, not nullable, , , )",
      "autoload (varchar(20), not nullable, MUL, default=yes, )"
    ],
    "relationships": []
  },
  "wp_postmeta": {
    "columns": [
      "meta_id (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "post_id (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "meta_key (varchar(255), nullable, MUL, , )",
      "meta_value (longtext, nullable, , , )"
    ],
    "relationships": []
  },
  "wp_posts": {
    "columns": [
      "ID (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "post_author (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "post_date (datetime, not nullable, , default=0000-00-00 00:00:00, )",
      "post_date_gmt (datetime, not nullable, , default=0000-00-00 00:00:00, )",
      "post_content (longtext, not nullable, , , )",
      "post_title (text, not nullable, , , )",
      "post_excerpt (text, not nullable, , , )",
      "post_status (varchar(20), not nullable, , default=publish, )",
      "comment_status (varchar(20), not nullable, , default=open, )",
      "ping_status (varchar(20), not nullable, , default=open, )",
      "post_password (varchar(255), not nullable, , , )",
      "post_name (varchar(200), not nullable, MUL, , )",
      "to_ping (text, not nullable, , , )",
      "pinged (text, not nullable, , , )",
      "post_modified (datetime, not nullable, , default=0000-00-00 00:00:00, )",
      "post_modified_gmt (datetime, not nullable, , default=0000-00-00 00:00:00, )",
      "post_content_filtered (longtext, not nullable, , , )",
      "post_parent (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "guid (varchar(255), not nullable, , , )",
      "menu_order (int(11), not nullable, , default=0, )",
      "post_type (varchar(20), not nullable, MUL, default=post, )",
      "post_mime_type (varchar(100), not nullable, , , )",
      "comment_count (bigint(20), not nullable, , default=0, )"
    ],
    "relationships": [
      {
        "column_name": "post_author",
        "referenced_table_name": "wp_users",
        "referenced_column_name": "ID"
      }
    ]
  },
  "wp_term_relationships": {
    "columns": [
      "object_id (bigint(20) unsigned, not nullable, PRI, default=0, )",
      "term_taxonomy_id (bigint(20) unsigned, not nullable, PRI, default=0, )",
      "term_order (int(11), not nullable, , default=0, )"
    ],
    "relationships": []
  },
  "wp_term_taxonomy": {
    "columns": [
      "term_taxonomy_id (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "term_id (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "taxonomy (varchar(32), not nullable, MUL, , )",
      "description (longtext, not nullable, , , )",
      "parent (bigint(20) unsigned, not nullable, , default=0, )",
      "count (bigint(20), not nullable, , default=0, )"
    ],
    "relationships": []
  },
  "wp_termmeta": {
    "columns": [
      "meta_id (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "term_id (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "meta_key (varchar(255), nullable, MUL, , )",
      "meta_value (longtext, nullable, , , )"
    ],
    "relationships": []
  },
  "wp_terms": {
    "columns": [
      "term_id (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "name (varchar(200), not nullable, MUL, , )",
      "slug (varchar(200), not nullable, MUL, , )",
      "term_group (bigint(10), not nullable, , default=0, )"
    ],
    "relationships": []
  },
  "wp_usermeta": {
    "columns": [
      "umeta_id (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "user_id (bigint(20) unsigned, not nullable, MUL, default=0, )",
      "meta_key (varchar(255), nullable, MUL, , )",
      "meta_value (longtext, nullable, , , )"
    ],
    "relationships": []
  },
  "wp_users": {
    "columns": [
      "ID (bigint(20) unsigned, not nullable, PRI, , auto_increment)",
      "user_login (varchar(60), not nullable, MUL, , )",
      "user_pass (varchar(255), not nullable, , , )",
      "user_nicename (varchar(50), not nullable, MUL, , )",
      "user_email (varchar(100), not nullable, MUL, , )",
      "user_url (varchar(100), not nullable, , , )",
      "user_registered (datetime, not nullable, , default=0000-00-00 00:00:00, )",
      "user_activation_key (varchar(255), not nullable, , , )",
      "user_status (int(11), not nullable, , default=0, )",
      "display_name (varchar(250), not nullable, , , )"
    ],
    "relationships": []
  },
  "wp_wpr_chats": {
    "columns": [
      "id (int(11), not nullable, PRI, , auto_increment)",
      "ft_user (varchar(255), not nullable, , , )",
      "ft_user_display_name (varchar(255), nullable, , , )",
      "ft_user_email (varchar(255), nullable, , , )",
      "ft_enabled (tinyint(1), nullable, , default=0, )",
      "is_hidden (tinyint(1), nullable, , default=0, )",
      "chat (longtext, nullable, , , )",
      "created_at (datetime, not nullable, , default=current_timestamp(), )"
    ],
    "relationships": []
  },
  "wp_wpr_ft": {
    "columns": [
      "id (int(11), not nullable, PRI, , auto_increment)",
      "ft_name (varchar(255), not nullable, , , )",
      "ft (longtext, not nullable, , , )",
      "created_at (datetime, not nullable, , default=current_timestamp(), )"
    ],
    "relationships": []
  },
  "wp_wprooster_chats": {
    "columns": [
      "id (int(11), not nullable, PRI, , auto_increment)",
      "ft_user (varchar(255), not nullable, , , )",
      "ft_user_display_name (varchar(255), nullable, , , )",
      "ft_user_email (varchar(255), nullable, , , )",
      "ft_enabled (tinyint(1), nullable, , default=0, )",
      "is_hidden (tinyint(1), nullable, , default=0, )",
      "chat (longtext, nullable, , , )",
      "created_at (datetime, not nullable, , default=current_timestamp(), )"
    ],
    "relationships": []
  },
  "wp_wprooster_ft": {
    "columns": [
      "id (int(11), not nullable, PRI, , auto_increment)",
      "ft_name (varchar(255), not nullable, , , )",
      "ft (longtext, not nullable, , , )",
      "created_at (datetime, not nullable, , default=current_timestamp(), )"
    ],
    "relationships": []
  }
}
